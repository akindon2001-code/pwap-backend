# Create an empty repo named  pwap-backend  on github.com/akindon2001-code
# (no README/gitignore/licence), then from inside the pwap_backend folder:

git init
git add .
git commit -m "PWAP backend reference implementation with tests and CI"
git branch -M main
git remote add origin https://github.com/akindon2001-code/pwap-backend.git
git push -u origin main

# If prompted for a password, use a Personal Access Token (GitHub no longer
# accepts account passwords over HTTPS):  github.com > Settings > Developer
# settings > Personal access tokens > Fine-grained token (repo scope).
